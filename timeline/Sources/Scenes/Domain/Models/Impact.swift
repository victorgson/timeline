
extension DefaultHapticBox {
    enum Impact {
        case light
        case medium
        case heavy
    }

    enum Notification {
        case success
        case warning
        case error
    }
}

